<!--ftcomponents/ftDragAndDrop/ftDragAndDrop.wxml-->
<wxs module="m1" src="./ftDragAndDrop.wxs"></wxs>
<view class="wrapper" data-disablemove="{{disableMove}}">
  <view wx:for="{{showList}}" wx:key="id" class="item item{{index}}" data-index="{{index}}">
    <view class="title">{{item.title}}</view>
    <view class="drag-btn drag-btn{{index}}" bind:longpress="{{m1.longPress}}" bind:touchmove="{{m1.touchMove}}" bind:touchend="{{m1.touchEnd}}" data-index="{{index}}" data-rect="{{rects[index]}}" data-rects="{{rects}}">
      拖放
    </view>
  </view>
</view>
