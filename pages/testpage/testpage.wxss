/* pages/testpage/testpage.wxss */
@keyframes overAnimation {
  0%{
    transform: scale(1,1);
  }
  100%{
    transform: scale(1,1);
  }
}
.banner{
  width: 100%;
  height: 400rpx;
  margin-top: -150rpx;
  animation: overAnimation 100000000s infinite;
}